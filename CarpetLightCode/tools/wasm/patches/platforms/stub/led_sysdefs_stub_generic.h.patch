--- /tmp/fastled_fresh_check3/platforms/stub/led_sysdefs_stub_generic.h	2026-08-05 05:26:41
+++ tools/wasm/vendor/fastled_src/platforms/stub/led_sysdefs_stub_generic.h	2026-08-05 05:19:23
@@ -52,6 +52,9 @@
 typedef volatile uint32_t RoReg;
 typedef volatile uint32_t RwReg;
 
+// PATCHED for CarpetLightCode's WASM build -- see FastLED.h's own patch
+// comment. Collides (different language linkage) with src/HalShim.h.
+#ifndef FASTLED_SKIP_WASM_ARDUINO_COMPAT
 extern "C" {
     void pinMode(uint8_t pin, uint8_t mode);
 
@@ -61,5 +64,6 @@
     void delay(int ms);
     void yield(void);
 }
+#endif // !FASTLED_SKIP_WASM_ARDUINO_COMPAT
 
 #endif // __INC_LED_SYSDEFS_STUB_H

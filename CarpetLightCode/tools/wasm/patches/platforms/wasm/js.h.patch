--- /tmp/fastled_fresh_check3/platforms/wasm/js.h	2026-08-05 05:26:41
+++ tools/wasm/vendor/fastled_src/platforms/wasm/js.h	2026-08-05 05:20:36
@@ -5,6 +5,10 @@
 
 // Needed or the wasm compiler will strip them out.
 // Provide missing functions for WebAssembly build.
+//
+// PATCHED for CarpetLightCode's WASM build -- see FastLED.h's own patch
+// comment. Collides (different language linkage) with src/HalShim.h.
+#ifndef FASTLED_SKIP_WASM_ARDUINO_COMPAT
 extern "C" {
 
 // Replacement for 'millis' in WebAssembly context
@@ -17,6 +21,7 @@
 void delay(int ms);
 void delayMicroseconds(int micros);
 }
+#endif // !FASTLED_SKIP_WASM_ARDUINO_COMPAT
 
 //////////////////////////////////////////////////////////////////////////
 // BEGIN EMSCRIPTEN EXPORTS
